//import javax.xml.soap.Node;

public class Program {
    
    public static void main(String argv[]) {
    	WebServer myServer = new WebServer();
    	myServer.runServer();
    	//Task task = new Task("user", "date", "time",  "content", "rcpt",  "subject", "isDone");
    	
    }
}